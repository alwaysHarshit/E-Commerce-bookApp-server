version: "1.0"

services:
  app:
    build: .
    image: catelog-service:1.0
    container_name: catelog-service
    ports:
      - "8082:8082"

    restart: on-failure
    networks:
      - micro-net

networks:
  micro-net:
    external: true